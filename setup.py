from setuptools import setup, find_packages

setup(
    name="api-gboard",
    version="1.0",
    packages=find_packages(),
    install_requires=[
        "python-dotenv==0.19.1",  # versão como exemplo; você pode querer a mais recente
        # ... você pode adicionar mais dependências aqui
    ],
)
